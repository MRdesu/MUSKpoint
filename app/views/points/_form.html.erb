<%= form_with(model: point, local: true) do |form| %>
  <% if point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
      <% point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :used_point %>
    <%= form.number_field :used_point, id: :point_used_point %>
  </div>

  <div class="field">
    <%= form.label :point_left %>
    <%= form.number_field :point_left, id: :point_point_left %>
  </div>

  <div class="field">
    <%= form.label :data_date %>
    <%= form.date_select :data_date, id: :point_data_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
